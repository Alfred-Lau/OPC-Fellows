# 稳定安装身份，不是产品线默认值。改掉会拆开已装用户和通知。
appId: tech.bitou.ownworkbuddy
productName: OPC-Fellows
directories:
  output: dist
  buildResources: build
beforeBuild: ./scripts/before-build.cjs
afterPack: ./scripts/after-pack.cjs
npmRebuild: false
files:
  - out/**/*
  - package.json
  - "!node_modules/**"
# dsh 的 node_modules 不能走 extraResources（builder 会丢掉），afterPack 再拷。
# P1：host / 内核包在 asar 外；spawn 仍用 afterPack 拷的 resources/dsh。
extraResources:
  - from: packages/opc-kernel
    to: opc-kernel
  - from: packages/occupation-social-ammo
    to: packages/occupation-social-ammo
  - from: build/dsh-host
    to: dsh-host
  - from: build/dsh-host/node_modules
    to: dsh-host/node_modules
asar: true
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
mac:
  icon: build/icon.icns
  # 签名身份走本机证书发现或环境变量，不要把姓名 / Team ID 写进仓库。
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  # 公证由 scripts/notarize.sh 单独跑，签名和公证解耦，重试不用重新打包。
  notarize: false
  gatekeeperAssess: false
  category: public.app-category.developer-tools
  extendInfo:
    NSUserNotificationsUsageDescription: OPC-Fellows 会在你设定的时间发送待办桌面通知。
  target:
    - dmg
    - zip
win:
  icon: build/icon.png
  target:
    - nsis
linux:
  target:
    - AppImage
