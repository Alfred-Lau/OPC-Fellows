# 项目不是主对话，也不是仓库

首页和左栏按「人 / 事」拆开：点成员进一对一主对话，用户开的多人才叫项目，今日是收件箱。监控里的 git 目录和已部署网站改称仓库 / 站点，避免和项目抢词。

不把所有 Thread 都叫项目，否则财务顾问的默认会话会混进项目列表，首页选择器和成员列表重名。也不把成员做成项目私有的孩子——单例职业的 Artifact 仍是一份真相，只是被叫进某条项目。主对话里 @ 第二人只提示去开项目；项目里没 @ 只有主成员听见。界面用「成员」，代码里仍是 Agent，见 ADR 0003。
