{
  "name": "opc-fellows-server",
  "version": "0.1.0",
  "private": true,
  "description": "OPC-Fellows 可选、自托管的参考后端：一个可读、可改、可扩展的起步模板，用来把你的全栈应用快速部署起来。",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "STORE=memory node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
